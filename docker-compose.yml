services:
  mysql:
    image: mysql:8.0  
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: kaddem
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  app:
    build: .  # Construit l'image localement depuis le Dockerfile
    container_name: kaddem-app
    depends_on:
      - mysql
    ports:
      - "8089:8089"

volumes:
  mysql_data:

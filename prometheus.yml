global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'  # Remove /kaddem prefix
    static_configs:
      - targets: ['app:8089']  # Use your actual host IP
    scrape_timeout: 10s
